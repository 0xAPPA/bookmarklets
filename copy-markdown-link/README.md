# copy-markdown-link

Copies the current page as a markdown link to the clipboard:

```
[Page title](https://example.com/path)
```

Falls back to a prompt with the text preselected if clipboard access is denied.

## Install

Create a new bookmark and paste the contents of [`bookmarklet.min.js`](bookmarklet.min.js) as its URL.
